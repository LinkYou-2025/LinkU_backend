# 모든 알림을 discord-alerts contact point로 라우팅
apiVersion: 1

policies:
  - orgId: 1
    receiver: discord-alerts
    group_by: ["alertname", "env"]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 1h
